source `dirname $0`/util.sh

# The syscallbuf depends on TLS, so with bad TLS, we just trigger an
# internal rr error.
skip_if_syscall_buf

record clone_bad_tls$bitness
replay
check 'EXIT-SUCCESS'
passed
